{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "python -m app.sync.scheduler horizon",
    "cronSchedule": "0 3 * * *",
    "restartPolicyType": "NEVER"
  }
}
